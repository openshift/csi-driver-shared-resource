apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: shared-resource-my-share
rules:
  - apiGroups:
      - storage.openshift.io
    resources:
      - sharedresources
    resourceNames:
      - my-share
    verbs:
      - get
      - list
      - watch